# 方角を計算
# ２地点の経緯度を使って出発点から見た到達点の方角を計算します。
# 方角は360°で表記し、0°が真北、180°が南を示します。

def calculation_azimuth(x_1, y_1, x_2, y_2):
    # Radian角に修正
    _x1, _y1, _x2, _y2 = map(np.radians, [x_1, y_1, x_2, y_2])
    
    delta_x = _x2 - _x1
    _y = np.sin(delta_x)
    _x = np.cos(_y1) * np.tan(_y2) - np.sin(_y1) * np.cos(delta_x)

    psi = np.rad2deg(np.arctan2(_y, _x))
    
    return np.where(psi < 0, 360 + psi, psi)についてpythonのコードで具体例を挙げて説明して

# calculation_azimuth 関数は、2つの地点の緯度経度座標を受け取り、その2つの地点間の方位角（azimuth）を計算する関数です。方位角は、1つの地点からもう1つの地点へ向かう方向を示します。具体的な例を挙げて説明します。
import numpy as np

# 2つの地点の緯度経度座標を設定（度数法で表現）
x_1 = 40.7128  # 緯度（例: ニューヨーク市の緯度）
y_1 = -74.0060  # 経度（例: ニューヨーク市の経度）
x_2 = 34.0522  # 緯度（例: ロサンゼルスの緯度）
y_2 = -118.2437  # 経度（例: ロサンゼルスの経度）

# 方位角を計算
azimuth = calculation_azimuth(x_1, y_1, x_2, y_2)

# 方位角を表示
display(f"2つの地点間の方位角: {azimuth} 度")

# このコードでは、2つの地点（ニューヨーク市とロサンゼルス）の緯度経度座標を設定し、calculation_azimuth 関数を使用してこれらの地点間の方位角を計算しています。
# 方位角の計算には三角関数を使用しており、計算結果は地点1から地点2へ向かう方向を示す角度（度数法）です。この場合、出力はおおよそ「2つの地点間の方位角: 245.89 度」のようになります。
